# Server-only. Never prefix this value with NEXT_PUBLIC_.
OPENAI_API_KEY=replace-with-a-server-side-key

# Optional. Defaults to gpt-5.4-mini.
OPENAI_MODEL=gpt-5.4-mini

# Optional canonical origin used by metadata (no trailing slash required).
NEXT_PUBLIC_SITE_URL=https://ai-tarot.timeflow.tw
