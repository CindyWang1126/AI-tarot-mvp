# Legacy Streamlit prototype

This folder preserves the pre-rebuild Streamlit implementation, original source JSON, and image assets for project history only.

It is not the production application. The production entrypoint is the Next.js app at the repository root.

The legacy image files have incomplete provenance documentation and are not loaded by the production UI.
